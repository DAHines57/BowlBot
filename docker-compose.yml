services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bowlbot
      POSTGRES_PASSWORD: bowlbot
      POSTGRES_DB: bowlbot_dev
    volumes:
      - bowlbot_pgdata:/var/lib/postgresql/data

volumes:
  bowlbot_pgdata:
